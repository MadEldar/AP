using System;

namespace Ap
{
    class Program
    {
        public int x = 10;
        public static dynamic y = "ASDF";
        static void Start(string[] args)
        {
            // Console.WriteLine(y);
            // Program p = new Program();
            // y = 20;
            // Console.WriteLine(args);
            // Console.WriteLine(p.x + y);
            // int[] b = {2, 3, 4};
            // dynamic[] a = {"qwerqwe", 1, "123", b, 123};
            // foreach (dynamic o in a)
            // {
            //     if (o is Array || o is String)
            //     {
            //         foreach (dynamic o1 in o)
            //         {
            //             Console.WriteLine(o1);
            //         }                    
            //     }
            // else
            // {
            // Console.WriteLine(o);
            // }
            // }
            // dynamic str = "";
            // while (!(str is int))
            // {
            //     str = Console.ReadLine();
            // }
            // Console.WriteLine(Convert.ToInt32(str));
        }
    }
}